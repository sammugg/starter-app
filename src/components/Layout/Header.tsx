import {
  AppBar,
  Typography,
} from '@mui/material';

function Header() {
  return (
    <AppBar position='static'>
      <Typography variant='h2'>Starter App</Typography>
    </AppBar>
  );
}

export default Header;
